// Single language configuration - French only
// Replaces next-intl configuration

// We only support French in single language mode
export const locales = ['fr'];

// French is not RTL (right-to-left)
export const isRtl = false;

// Default locale
export const defaultLocale = 'fr';
